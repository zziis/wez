وصل - تحديث الواجهة والملاحة V2.2
================================

ما تم تنفيذه:
- واجهة رئيسية جديدة قريبة من نمط تطبيقات الملاحة الحديثة.
- طلب إذن الموقع عند الدخول وتحديد نقطة الانطلاق من GPS الحقيقي.
- تحديث موقع المستخدم مباشرة أثناء الحركة.
- بحث شبكي عن الأماكن داخل العراق عبر OpenStreetMap Nominatim عند الضغط على «بحث».
- إنشاء مسار حقيقي عبر شبكة الطرق باستخدام OSRM مع الوقت والمسافة وعلامة الوصول.
- واجهة ملاحة أثناء القيادة مع اتجاه المناورة والمسافة والسرعة والوقت المتبقي.
- مساعد صوتي عربي باستخدام أصوات النظام المتوفرة على الجهاز.
- تنبيه صغير «لا تكتب وأنت تسوق» أثناء الملاحة/الحركة عند فتح البحث.
- حفظ مكان أو رحلة على الجهاز واستخدام الرحلة المحفوظة عند عدم وجود إنترنت.
- Service Worker لتخزين ملفات التطبيق وبيانات الخريطة التي سبق عرضها قدر الإمكان.
- حالة اتصال حقيقية من navigator.onLine ورسالة واضحة عند انقطاع الشبكة.
- جلب كاميرات السرعة ومهدئات السرعة/المطبات المسجلة في OpenStreetMap حول الموقع عند توفرها.
- تبليغات المستخدم تحفظ محلياً حالياً على الجهاز.

مهم:
- يجب نشر التطبيق عبر HTTPS حتى يعمل GPS وService Worker بشكل صحيح في المتصفحات الحديثة.
- الحوادث والازدحام اللحظي بين جميع المستخدمين تحتاج API/قاعدة بيانات مشتركة ومصدر بيانات مرور حي.
- خدمة OSRM العامة مناسبة للاختبار. للإنتاج الكبير يفضل خادم/مزود Routing مخصص.
- Nominatim العام يستخدم هنا عند تنفيذ البحث صراحة وليس كبحث تلقائي مع كل حرف.

تحديث 2.2:
- إلغاء الاعتماد على مزود الخرائط القديم نهائياً.
- اعتماد OpenFreeMap + MapLibre بدون API Key.
- إزالة أسماء الشوارع والمناطق المطبوعة من طبقة الخريطة الأساسية.
- إبقاء المسار، موقع المستخدم، وجهة الوصول، والتنبيهات فوق الخريطة.
- رفع رقم كاش Service Worker إلى v2.2 لمسح البلاطات والملفات القديمة تلقائياً.
- إضافة طبقة احتياط Esri أساسية بدون طبقة أسماء منفصلة إذا تعذر تحميل MapLibre.


=== V2.3 DEPLOY FIX ===
- This ZIP is deploy-ready: index.html is at the ZIP root, not inside a nested folder.
- Old Wasil Service Worker/cache is cleared once automatically.
- Local CSS/JS references use v=2.3.0 cache-busting.
- Service Worker now prefers the network for same-origin app files, with cache only as offline fallback.
- force-update.html is included. After uploading all files, open /force-update.html once on the live site if an old version is still visible.
- مزود الخريطة القديم غير مستخدم في هذه الحزمة.
